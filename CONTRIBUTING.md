# Contributing

This is a lab repo. If you fork it:

- Keep `private/` gitignored for raw evidence.
- Prefer publishing only `config/` templates + `docs/` + scripts.
- If you add new checks, update `docs/TESTING.md` accordingly.
